- name: Configure DNS Resolvers
  hosts: all
  tasks:
    - name: Update resolv.conf
      ansible.builtin.copy:
        dest: /etc/resolv.conf
        content: |
          nameserver 8.8.8.8
          nameserver 4.2.2.1
          search mydomain.com
